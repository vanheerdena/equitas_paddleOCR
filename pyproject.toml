[project]
name = "paddleocr-fastapi-service"
version = "0.1.0"
description = "FastAPI wrapper for PaddleOCR text and table extraction"
requires-python = ">=3.10,<3.12"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
